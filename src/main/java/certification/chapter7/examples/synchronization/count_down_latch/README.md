###CountDownLatch 
is used to start a series of threads and then wait until all of them are complete 
(or until they call countDown() a given number of times.

Use CountDownLatch to wait for completion of all threads